{
  "name": "tms-monorepo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=apps/public\" \"npm run dev --workspace=apps/admin\" \"npm run dev --workspace=apps/api\"",
    "dev:public": "npm run dev --workspace=apps/public",
    "dev:admin": "npm run dev --workspace=apps/admin",
    "dev:api": "npm run dev --workspace=apps/api",
    "build": "npm run build --workspace=apps/public && npm run build --workspace=apps/admin && npm run build --workspace=apps/api",
    "build:public": "npm run build --workspace=apps/public",
    "build:admin": "npm run build --workspace=apps/admin",
    "build:api": "npm run build --workspace=apps/api",
    "start:api": "node apps/api/dist/src/main",
    "start:prod": "node apps/api/dist/src/main",
    "lint": "oxlint"
  },
  "devDependencies": {
    "concurrently": "^9.2.4",
    "oxlint": "^1.75.0"
  },
  "dependencies": {
    "axios": "^1.19.0"
  }
}
